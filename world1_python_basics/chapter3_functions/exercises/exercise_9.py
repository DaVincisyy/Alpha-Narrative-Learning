"""
练习 9：计算平均价格

任务：计算股票的平均价格
参数：
    stocks: 股票列表，每个元素是字典，包含 'price'

返回：
    平均价格（浮点数）
    如果列表为空，返回 0

示例：
    stocks = [
        {'code': 'AAPL', 'price': 100},
        {'code': 'GOOGL', 'price': 200},
        {'code': 'MSFT', 'price': 300}
    ]
    calculate_average_price(stocks)
    # 返回: 200.0
"""

def calculate_average_price(stocks):
    """
    计算股票的平均价格

    参数:
        stocks: 股票列表，每个元素是字典，包含 'price'

    返回:
        平均价格（浮点数）
        如果列表为空，返回 0

    示例:
        stocks = [
            {'code': 'AAPL', 'price': 100},
            {'code': 'GOOGL', 'price': 200},
            {'code': 'MSFT', 'price': 300}
        ]
        calculate_average_price(stocks)
        # 返回: 200.0
    """
    pass
