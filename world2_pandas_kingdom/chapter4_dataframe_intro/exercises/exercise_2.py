"""
练习 2：查看 DataFrame 信息

任务：获取 DataFrame 的基本信息
要求：返回 DataFrame 的行数和列数
"""

import pandas as pd


def get_dataframe_shape(df):
    """
    获取 DataFrame 的形状

    参数:
        df (DataFrame): 输入的 DataFrame

    返回:
        tuple: (行数, 列数)

    示例:
        >>> df = pd.DataFrame({'A': [1, 2, 3], 'B': [4, 5, 6]})
        >>> get_dataframe_shape(df)
        (3, 2)
    """
    # 在这里写你的代码
    # 提示：使用 df.shape 属性
    pass
