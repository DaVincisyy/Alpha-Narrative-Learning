"""
练习 8：综合练习 - 筛选股票新闻

任务：从新闻数据中筛选高质量的正面新闻
要求：筛选满足以下条件的新闻：
     - 情绪分数 > 0.7
     - 阅读量 > 5000
     - 标题包含 '业绩' 或 '增长'
"""

import pandas as pd


def filter_quality_news(df):
    """
    筛选高质量的正面新闻

    参数:
        df (DataFrame): 包含 '标题', '情绪分数', '阅读量' 列的 DataFrame

    返回:
        DataFrame: 满足条件的新闻

    示例:
        >>> df = pd.DataFrame({
        ...     '标题': ['公司业绩大涨', '市场波动', '增长超预期', '业绩下滑'],
        ...     '情绪分数': [0.85, -0.3, 0.9, -0.5],
        ...     '阅读量': [10000, 3000, 8000, 6000]
        ... })
        >>> result = filter_quality_news(df)
        >>> len(result)
        2
    """
    # 在这里写你的代码
    # 步骤：
    # 1. 筛选情绪分数 > 0.7
    # 2. 筛选阅读量 > 5000
    # 3. 筛选标题包含 '业绩' 或 '增长'
    # 4. 组合所有条件
    pass
