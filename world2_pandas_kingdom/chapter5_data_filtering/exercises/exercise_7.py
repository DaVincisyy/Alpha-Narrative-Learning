"""
练习 7：复杂条件组合

任务：使用复杂的条件组合筛选数据
要求：筛选满足以下条件的股票：
     - 价格在 [10, 30] 范围内
     - 涨跌幅大于 0
     - 股票代码不是 '000003'
"""

import pandas as pd


def filter_complex_conditions(df):
    """
    使用复杂条件筛选股票

    参数:
        df (DataFrame): 包含 '股票代码', '价格', '涨跌幅' 列的 DataFrame

    返回:
        DataFrame: 满足所有条件的行

    示例:
        >>> df = pd.DataFrame({
        ...     '股票代码': ['000001', '000002', '000003', '000004'],
        ...     '价格': [15, 25, 20, 35],
        ...     '涨跌幅': [0.05, 0.03, 0.02, 0.01]
        ... })
        >>> result = filter_complex_conditions(df)
        >>> list(result['股票代码'])
        ['000001', '000002']
    """
    # 在这里写你的代码
    # 提示：组合多个条件，使用 & 连接
    pass
