"""练习 1：计算平均值"""
import pandas as pd

def calculate_mean(df, column_name):
    """计算指定列的平均值

    参数:
        df (DataFrame): 输入的 DataFrame
        column_name (str): 列名

    返回:
        float: 平均值

    示例:
        >>> df = pd.DataFrame({'价格': [10, 20, 30]})
        >>> calculate_mean(df, '价格')
        20.0
    """
    pass
